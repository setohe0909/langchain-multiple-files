# Langchain chat with multiple PDFs

## Usage

1- Install dependecies
command:
   ```
   pip install -r requirements.txt
   ```

2-- Run the `main.py` file using the Streamlit CLI. Execute the following command:
   ```
   streamlit run app.py
   ```
